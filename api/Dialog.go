package main

import (
	"models"
)

// GenerateOutgoingMessage formulates a message given a user's recent request
func GenerateOutgoingMessage(user models.User) (message string, err error) {
	message = "Hey there!"
	return
}
